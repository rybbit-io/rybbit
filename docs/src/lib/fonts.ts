import { Tilt_Warp } from "next/font/google";

// Brand display face — used for the hero and landing section headings.
// Import from here so every surface shares one instance.
export const tiltWarp = Tilt_Warp({
  subsets: ["latin"],
});
